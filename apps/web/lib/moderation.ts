/**
 * How many games one creator may have waiting for review at once. Shared by the
 * template wizard and the studio, so submitting a code game counts toward the
 * same limit as publishing a template game.
 */
export const PENDING_LIMIT = 3;
